import { combineReducers } from "redux";
import numberIncrementerDecrementer from "./IncrementerDecrementer";

const rootReducer = combineReducers({
  numberIncrementerDecrementer,
});

export default rootReducer;